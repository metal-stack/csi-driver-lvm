apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lvm-pvc-remount
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: csi-driver-lvm-linear
  resources:
    requests:
      storage: 100Mi
