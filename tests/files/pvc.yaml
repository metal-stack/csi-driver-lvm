apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lvm-pvc-block
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Block
  storageClassName: csi-lvm-sc-linear
  resources:
    requests:
      storage: 10Mi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lvm-pvc-linear
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: csi-lvm-sc-linear
  resources:
    requests:
      storage: 10Mi

