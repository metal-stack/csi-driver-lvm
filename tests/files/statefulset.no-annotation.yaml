apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: nginx-no-annotation
  labels:
    app: nginx-no-annotation
spec:
  serviceName: "nginx-no-annotation" 
  replicas: 1
  selector:
    matchLabels:
      app: nginx-no-annotation
  template:
    metadata:
      labels:
        app: nginx-no-annotation
    spec:
      containers:
      - name: nginx
        image: nginx:1.14.2
        ports:
        - containerPort: 80
        volumeMounts:
        - mountPath: "/data"
          name: my-csi-volume
  volumeClaimTemplates:
  - metadata:
      name: my-csi-volume
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 1Gi
      storageClassName: csi-driver-lvm-linear
