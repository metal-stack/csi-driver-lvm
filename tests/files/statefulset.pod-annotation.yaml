apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: nginx-pod-annotation
  labels:
    app: nginx-pod-annotation
spec:
  serviceName: "nginx-pod-annotation" 
  replicas: 1
  selector:
    matchLabels:
      app: nginx-pod-annotation
  template:
    metadata:
      annotations:
        metal-stack.io/csi-driver-lvm.is-eviction-allowed: "true"
      labels:
        app: nginx-pod-annotation
    spec:
      containers:
      - name: nginx
        image: nginx:1.14.2
        ports:
        - containerPort: 80
        volumeMounts:
        - mountPath: "/data"
          name: my-csi-volume
  volumeClaimTemplates:
  - metadata:
      name: my-csi-volume
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 1Gi
      storageClassName: csi-driver-lvm-linear
