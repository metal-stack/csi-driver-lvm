apiVersion: v1
kind: Pod
metadata:
  name: volume-encrypted-reading-test
spec:
  containers:
  - name: volume-encrypted-reading-test
    image: alpine
    imagePullPolicy: IfNotPresent
    command:
      - tail
      - -f
      - /remount/output.log
    securityContext:
      allowPrivilegeEscalation: false
      runAsNonRoot: true
      runAsUser: 10014
      seccompProfile:
        type: RuntimeDefault
      capabilities:
        drop:
          - ALL
    volumeMounts:
    - name: encrypted-remount
      mountPath: /remount
    resources:
      limits:
        cpu: 100m
        memory: 100M
  volumes:
  - name: encrypted-remount
    persistentVolumeClaim:
      claimName: lvm-pvc-encrypted-linear
