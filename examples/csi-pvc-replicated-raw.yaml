kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: pvc-replicated-raw
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: csi-driver-lvm-replicated
  volumeMode: Block
  resources:
    requests:
      storage: 1Gi
