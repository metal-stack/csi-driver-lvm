apiVersion: apps/v1
kind: Deployment
metadata:
  name: csi-deployment-replicated
  labels:
    app: csi-app-replicated
spec:
  replicas: 1
  selector:
    matchLabels:
      app: csi-app-replicated
  template:
    metadata:
      labels:
        app: csi-app-replicated
    spec:
      containers:
        - name: csi-app
          image: busybox
          volumeMounts:
          - mountPath: "/data"
            name: my-csi-volume
          command: [ "sleep", "1000000" ]
      volumes:
        - name: my-csi-volume
          persistentVolumeClaim:
            claimName: csi-pvc-replicated
