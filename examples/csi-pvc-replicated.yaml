apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: csi-pvc-replicated
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: csi-driver-lvm-replicated
