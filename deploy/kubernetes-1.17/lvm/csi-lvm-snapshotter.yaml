kind: Service
apiVersion: v1
metadata:
  name: csi-lvm-snapshotter
  labels:
    app: csi-lvm-snapshotter
spec:
  selector:
    app: csi-lvm-snapshotter
  ports:
    - name: dummy
      port: 12345

---
kind: StatefulSet
apiVersion: apps/v1
metadata:
  name: csi-lvm-snapshotter
spec:
  serviceName: "csi-lvm-snapshotter"
  replicas: 1
  selector:
    matchLabels:
      app: csi-lvm-snapshotter
  template:
    metadata:
      labels:
        app: csi-lvm-snapshotter
    spec:
      affinity:
        podAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - csi-lvmplugin
            topologyKey: kubernetes.io/hostname
      serviceAccount: csi-snapshotter
      containers:
        - name: csi-snapshotter
          image: quay.io/k8scsi/csi-snapshotter:v2.0.0
          args:
            - -v=5
            - --csi-address=/csi/csi.sock
          securityContext:
            # This is necessary only for systems with SELinux, where
            # non-privileged sidecar containers cannot access unix domain socket
            # created by privileged CSI driver container.
            privileged: true
          volumeMounts:
            - mountPath: /csi
              name: socket-dir
      volumes:
        - hostPath:
            path: /var/lib/kubelet/plugins/csi-lvm
            type: DirectoryOrCreate
          name: socket-dir
